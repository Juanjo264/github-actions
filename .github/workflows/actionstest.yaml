name: Deploy test

on: 
  push:
    branches: [ "main" ]

jobs:

  firstjob:
    runs-on: ubuntu-latest
    steps:
      - name: print variable
        run: echo  "${{ secrets.TOKEN }}"

      - name: setup
        uses: actions/setup-node@v4
        with:
          node-version: '18'